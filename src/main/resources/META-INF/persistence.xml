<persistence xmlns="https://jakarta.ee/xml/ns/persistence" version="3.0">

    <persistence-unit name="talentoPU">

        <!-- Proveedor Hibernate -->
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- ENTIDADES -->
        <class>model.PublicServer</class>
        <class>model.Position</class>
        <class>model.Dependency</class>
        <class>model.EmploymentLink</class>
        <class>model.AdministrativeSituation</class>
        <class>model.Vacation</class>
        <class>model.Permission</class>
        <class>model.License</class>

        <properties>
            <!-- CONEXIÓN -->
            <property name="jakarta.persistence.jdbc.url" value="jdbc:postgresql://localhost:55439/talento_humano"/>
            <property name="jakarta.persistence.jdbc.user" value="postgres"/>
            <property name="jakarta.persistence.jdbc.password" value="talento_seguro_2026"/>
            <property name="jakarta.persistence.jdbc.driver" value="org.postgresql.Driver"/>

            <!-- AUTO CREATE TABLES -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>

            <!-- DEBUG -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
        </properties>

    </persistence-unit>

</persistence>